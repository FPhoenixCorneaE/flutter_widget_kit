name: widget_kit
description: Flutter UI Kit forked from GetWidget — 1000+ production-ready widgets (buttons, cards, carousels, forms, and more).
version: 7.0.2
homepage: https://github.com/FPhoenixCorneaE/flutter_widget_kit

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - icons/
